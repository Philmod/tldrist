# Cloud Build configuration for Pull Request checks
# This runs tests only (no deployment)
steps:
  # Run tests
  - name: ghcr.io/astral-sh/uv:python3.11-bookworm-slim
    entrypoint: bash
    args:
      - -c
      - |
        uv sync --extra dev && uv run pytest tests/unit -v

options:
  logging: CLOUD_LOGGING_ONLY
